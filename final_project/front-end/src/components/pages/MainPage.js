import React from "react";
import HeaderContainer from "../common/HeaderContainer";

const MainPage = () => {
  return (
    <React.Fragment>
      <HeaderContainer />
    </React.Fragment>
  );
};

export default MainPage;
